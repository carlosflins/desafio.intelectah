@model IEnumerable<Desafio.Domain.Entities.TipoExame>
@{
    ViewData["Title"] = "Cadastro de Tipos de Exame";
}
<div class="mt-3">
    <h4>@ViewData["Title"]</h4>
</div>

<div class="row mt-3">
    <div class="mb-3">
        <a asp-controller="TipoExame" asp-action="Create" class="btn btn-primary mx-0">Novo Tipo de Exame</a>
    </div>

    <div>
        <table id="datatable" class="table table-striped table-bordered" style="width:100%">
            <thead class="table-dark">
                <tr>
                    <th>Nome</th>
                    <th>Descrição</th>
                    <th></th>
                </tr>
            </thead>

            <tbody>
                @{
                    if (Model.Count() <= 0)
                    {
                        <tr>
                            <td colspan="3">Nenhum Tipo de Exame cadastrado.</td>
                        </tr>
                    }
                }
                @foreach(var obj in Model)
                {
                    <tr>
                        <td>@obj.Nome</td>
                        <td>@obj.Descricao</td>
                        <td>
                            <div class="btn-group">
                                <a asp-controller="TipoExame" 
                                    asp-action="Details" 
                                    asp-route-id="@obj.Id"
                                    title="Detalhes"
                                    class="btn btn-info mx-0">
                                    <i class="bi bi-zoom-in" alt="Detalhes"></i>
                                </a>
                                <a asp-controller="TipoExame" 
                                    asp-action="Update" 
                                    asp-route-id="@obj.Id"
                                    title="Alterar"
                                    class="btn btn-primary mx-0">
                                    <i class="bi bi-pencil-square"></i>
                                </a>
                            <a asp-controller="TipoExame"
                           asp-action="Delete" 
                                    asp-route-id="@obj.Id"
                                    title="Apagar"
                                    class="btn btn-danger mx-0">
                                    <i class="bi bi-trash-fill"></i>
                                </a>
                            </div>
                        </td>
                    </tr>
                }
            </tbody>
        </table>
    </div>
</div>
